const arr: number[] = []
const matrix: number[][] = [ [], [] ]
const arr2: (number | string)[] = [ 1, '2'  ]

arr.map((item) => item + 1 )

// Tuples (Кортежи)

const coordinates: [number, number] = [60.2121, 63.4841]